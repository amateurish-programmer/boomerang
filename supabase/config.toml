project_id = "boomerang"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 17

[auth]
enabled = true
site_url = "https://skeghmapzrmahxehazlp.supabase.co"
enable_signup = true

[auth.email]
enable_signup = true
enable_confirmations = true

[functions.api]
# The handler validates every user JWT against Auth before using any service credential.
# Explicit validation also supports projects using asymmetric signing keys.
verify_jwt = false
