# 0.3.1 发布前验收计划

延续 docs/DEVELOPMENT_TASKS.md 的 P3/P4/P8 验收，不改变已批准产品范围。用户2026-09-19要求继续；百炼仍暂缓。保留本机未跟踪 puppet-dance.html，不修改或暂存。

1. P8系统边界：真实 Android DocumentsUI/ContentResolver 导出、选择文件、导入预览与确认；取消不得修改数据，账号切换旧回调不得串账号；生成回旋卡并校验真实FileProvider/分享Intent，截图供视觉核验。修复观察到的问题，追加行为测试。
2. P4系统边界：通知权限拒绝/开启/从设置返回后的显示，Worker真实通知与去重、失效记录取消、账号切换取消和点击边界；增加设备用例，修复观察到的问题。
3. 集成与兼容性：Android API26/33/35 云端设备矩阵，收集JUnit和截图；对应测试、Lint、构建、合约及后端检查；修复新发现失败，独立复查。
4. 交付：版本0.3.1/versionCode4，同一独立证书签名；Git阶段提交和云端构建，报告明确本轮与供应商/真机待验收边界。真实服务只核对配置与已部署状态，不发送邮件，不启用Cron。

设备测试不等于真机验收；真实供应商和邮件需要外部条件，不伪造成功。完成所有可独立推进的验收后交付剩余项列表。
