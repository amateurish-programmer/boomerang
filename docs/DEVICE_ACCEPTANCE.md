# 真机验收记录

更新：2026-09-26。本表区分已执行与待验收；只使用专用测试账号和合成内容。当前手机vivo V2430A、Android16/API36、0.4.1/code6、Asia/Shanghai。自动化模拟器报告另见阶段集成报告，本次证据见 [验收报告](reports/ACCEPTANCE-2026-09-26.md)。

| 项目 | 操作与预期 | 实际结果 |
|---|---|---|
| 应用内升级 | 同证书覆盖安装，不卸载、不清数据；升级后核对专用记录与安装包身份。 | 9月19日已通过0.3.0→USB引导0.4.0→应用内OTA 0.4.1，专用记录保留、摘要/证书一致，见[OTA报告](reports/OTA.md)。不外推所有账号数据或中途取消 |
| 新建、日期与历史 | 创建当天到期的合成记录，详情显示今天到期与第1版历史；编辑后旧版保留。 | 9月26日通过新建、当天显示、编辑及旧原话回看；胶囊封存与追加备注后1–4版保留，失败修改未新增历史 |
| 邮箱与账号隔离 | 在应用注册并由邮箱本人完成验证；登录A创建记录，退出后登录B，A记录不可见；匿名数据仅明确预览选择导入后归属账号。 | 单台vivo A/B切换、默认空库、明确勾选1条本机记录导入、A上传/回读1条、B同步0条、本机3条恢复通过。使用管理员预确认合成账号，没有验证邮箱投递 |
| 双设备离线冲突 | 两台手机登录同一专用账号，先同步同一条合成记录；分别断网修改，再依次同步。冲突保留双方内容和历史，由用户选择，失败不能丢失本地修改。 | 真机待验收；9月26日真实HTTPS同用户双会话409、明确重试、幂等及三版本历史已通过，不能替代双手机离线UI |
| 通知拒绝与恢复 | Android13+实际拒绝通知权限；到期提醒仍在通知中心。点通知设置开启后返回，关闭提示消失；新提醒可投递，已拒绝时保存的旧提醒不补发重复提示。 | 待验收 |
| 系统通知点击 | 点通知进入准确记录；切换账号后旧通知不得进入新账号相同编号的记录，旧账号任务应取消。 | 通知中心及系统通知实际点击均进入专用记录；系统通知不含原话。账号内通知中心隔离通过；旧通知跨账号点击路由未单独执行 |
| 后台与重启 | 创建到期前7天、1天及当天的合成记录，记录系统省电设置、锁屏/重启/长期后台的实际提醒时间。WorkManager不承诺准点；回前台应补查，重复扫描不连续补发三条。 | 待验收 |
| 修改与胶囊 | 修改截止日、取消或删除后旧系统提醒撤销；封存胶囊后原话、日期及验证标准不可改，仍可追加说明，历史保留。 | 胶囊确认取消、次日零点解锁约定、三类受锁字段分别拒绝、失败草稿保留、仅追加备注成功及历史通过；延期/取消/删除撤销旧系统提醒待真机验收 |
| 系统文件提供方 | 分别用手机自带文件管理器、实际使用的云盘导出再导入。取消不改数据；预览后选择跳过/替换才写入；账号切换后旧选择器返回不得串账号。 | DocumentsUI本地导出、vivo文件选择器回选、取消、3条重复预览、跳过导入0条及专用历史不变通过。全量替换、云盘和跨账号旧选择器返回未在此真机执行 |
| 回旋卡 | 预览中文长文排版与来源，私人备注不得出现；只有本人明确选择系统分享目标后才发送。记录所用分享目标，不替别人发送测试信息。 | 专用记录图片目视检查、私人备注排除、系统Chooser打开并取消通过；中文长文/复杂来源、外部接收仍待验证，未发送消息 |

百炼真实 analyze/chat/search/extractor/周报，以及 Cron→复核→通知→用户确认，是另一组依赖服务配置的验收。用户暂缓百炼期间，保持生产 Cron 关闭，不把合成响应测试或模拟器通过视为真实供应商通过。
