# P1 Android 骨架

范围：四导航、中文空态、新建和详情结构、日夜主题、SavedStateHandle 保留输入与导航、原生回旋镖图标、3项 instrumentation 用例、GitHub Actions。

版本 0.1.0。此阶段没有真实 CRUD，保存按钮明确禁用；不会把演示数据写入正式库。

本机验证：XML 解析通过；由于 SDK35 缺失，Gradle 未进入 Kotlin 编译。完整编译、JVM、lint、APK 改由云端执行。instrumentation 需模拟器，不与编译验收混同。

当前状态：CI 待执行，结果随后补充。P2 在云端 P1 检查同时开发，阶段通过必须等相应检查完成。
