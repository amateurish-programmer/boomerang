# 剩余阶段验收：2026-09-26

用户授权继续分阶段自动推进；此前阶段后停止的约定已被取代，百炼暂不配置的决定保留。本轮基线main `e5aa02e9ca8b6c3b2ecb5e5acccc5e6e9a3a958f`，已发布Android 0.4.1/code6。未修改产品代码、数据库结构或生产调度，未重新发布APK。

## 阶段1–2：本机功能与提醒

vivo V2430A，Android16/API36，ADB设备已授权，实际本机日期2026-09-26，记录时区Asia/Shanghai。创建专用记录 `ACCEPT_20260926_DEVICE_RECORD`，目标、人物“我”、起止2026-09-26、精度DAY、标准 `ACCEPT_verify_backup_history_reminder`。保存后详情显示“今天到期”，修改历史显示第1版 `2026-09-26T00:54:58.789244Z`。

新建与日期显示已实测；尚未进行编辑历史、备份、回旋卡、胶囊、通知点击及后台验收。继续操作时发现前台已切至机器人控制页面，立即停止手机操作并询问可用窗口。后续必须先读取当前前台，确认回旋镖页面后再触控，不依据先前坐标直接操作。没有卸载或清除应用，也没有完成备份、跨账号切换或发送分享。

### 手机恢复可用后的续验

以下结果取代上段暂停时的未验收状态。用户再次指示继续后，先核对前台为回旋镖，再恢复操作；每次触控前增加前台应用检查。

- 编辑原话为 `ACCEPT_20260926_DEVICE_RECORD_EDITED`、备注为专用隐私标记，生成第2版；展开第1版仍见原始原话。
- 通知中心显示版本1、版本2各自的当天提醒；点击版本2打开准确记录。系统通知标题“回旋镖到期提醒”、正文“今天到期”，未暴露原话；实际点击系统通知同样打开该记录。未强制重置通知权限，因此本次未执行拒绝权限路径。
- 本机文件导出取消、导入选择取消和导入预览取消均返回正常。DocumentsUI将JSON保存到Downloads的 `boomerang-acceptance-20260926.json`，vivo系统文件选择器能够回选该文件。预览识别3条记录、3条UUID重复；“跳过重复并导入”返回0条，总数保持3条，专用记录仍仅第1/2版。未执行全文件替换，未把完整备份复制到电脑或Git。
- 目视检查专用回旋卡PNG：原话、日期、未确认状态及无来源提示正常，私人备注标记不存在。系统Chooser已实际打开后返回取消，没有选择接收应用或发送消息。未以此声称外部接收成功或覆盖长中文与复杂来源排版。
- 胶囊确认框取消后仍可封存；确认后解锁时间为 `2026-09-26T16:00:00Z`（Asia/Shanghai次日零点），生成第3版。分别尝试改原话、将起止日期同时改到2026-10-04、改验证标准，三次保存均提示“时间胶囊尚未到期，只能补充备注”，失败草稿保留。逐一恢复受锁字段后仅追加备注，成功生成第4版；原话、期限、标准及解锁约定保持不变。最终历史恰为1–4版，三次失败未增加版本。

受控新证据位于同一phone目录：`history-original.xml`、`post-backup-history.xml`、`notification-center.xml`、`system-notification-open.xml`、`import-skipped.xml`、`share-preview.png`、`capsule-*-error.xml`、`capsule-note-saved.xml`、`capsule-final-history.xml`。含其他文件名、联系人或非测试记录的临时选择器/预览XML不纳入验收归档并已删除。

短时退到桌面67秒后返回：同一系统通知键保持1条，通知中心版本1、2、4各1条，没有同版本重复；胶囊封存本身未立即扫描，所以未声称版本3发过提醒。数据和4版历史保留。用户随后明确授权整机重启及锁屏测试：09:35:45发起重启，用户完成首次解锁；09:37:52核对启动计数80→81、sys.boot_completed=1，回旋镖仍显示正确原话、期限、胶囊解锁约定及1–4版历史。09:38:36开始锁屏观察，09:49:02结束，实际626秒，连接USB供电，检查为Asleep；没有修改省电策略或强制执行后台任务。用户解锁后记录、胶囊、1–4版历史及通知中心同版唯一性保留；不将其外推为跨日或长期不供电投递。

本机受控XML证据保存在忽略目录 `.tools/acceptance-20260926/phone/` 的 `saved-due.xml`、`detail-more.xml`、`detail-top.xml`。专用记录暂留手机用于继续验收；未将其他应用页面归入交付证据。全库备份包含私人内容，后续只保存本地，不提交Git；完整备份仅测试取消及跳过重复，不执行全量替换。

## 阶段3：真实云端账号与同步

已完成10组真实HTTPS断言。独立项目 `skeghmapzrmahxehazlp`，创建两个随机example.invalid账号，以管理员预确认方式避免发送邮件；A使用两个独立登录会话，B用于隔离验证。

| 断言 | 实际结果 |
|---|---|
| A1/A2独立会话 | 同一用户、不同访问令牌，认证通过 |
| 创建与操作重放 | 两次返回revision1；实际未人为丢弃网络响应 |
| 双用户隔离 | A可读取自己的记录；B读取不到 |
| A1更新 | revision1→2 |
| A2旧版本更新 | HTTP409；A1内容不被覆盖 |
| A2明确重试 | 基于revision2得revision3，重复提交仍为3，三个原文版本全部保留 |
| 来源同步 | 重试幂等，B不能读取A来源 |
| 来源历史与原子快照 | 记录/来源及修订一致 |
| 越权操作 | 他人快照、无归属候选确认均HTTP403 |
| API边界 | 未登录analyze为401；已登录但未配百炼为503 NOT_CONFIGURED；普通用户调用worker为401 |

08:57完成测试。清理限定生成UUID、用途/本次run元数据及合成邮箱后缀，在单一事务中执行，锁等待2秒、语句15秒上限。为删除合成历史，独占锁内暂时关闭两项历史不可修改触发器并在同一事务恢复；失败会回滚，不留下持久变更。08:58独立只读查询确认该批次用户、会话、记录、来源、两类历史及幂等操作残余全部为0，两项历史保护触发器均启用。未查询或修改真实用户内容。

已脱敏原始结果：`.tools/acceptance-20260926/cloud-smoke-result.json`、`cleanup-verification.json`；持久汇总见同报告目录的 `ACCEPTANCE-2026-09-26-results.json`。这是后端双会话验收；后续单台真机账号切换另列，邮箱投递及两台物理手机离线冲突仍需各自证据。

### 单台vivo手机账号切换、显式导入与真实同步

在确认手机处于未登录本机空间后，另外创建两个随机、管理员预确认的example.invalid账号A/B，不发送邮件。凭据仅暂存本项目加密文件，手机登录输入后立即删除临时凭据页面XML，未打印密码或令牌。

1. 手机登录A，镖库显示“这里还没有记录”，通知中心为空；独立云端范围查询确认A/B均0条记录、0修订、0来源，本机3条内容没有自动上传。
2. 本机导入预览中只勾选 `ACCEPT_20260926_DEVICE_RECORD_EDITED`，其余两条均未勾选。“确认导入1条”后手动同步，手机显示上传1、读取1、冲突0。
3. 云端精确范围查询确认A恰有1条专用记录、0条非测试记录，原话、起止日期、胶囊解锁时间、私人备注均精确匹配；该账号云端修订1条、来源0条，不将其描述为本机4版历史全部上传。B仍为0条。
4. 退出A、登录B，镖库为空；B真实手动同步上传0、读取0、冲突0，未看到A或原本机内容。
5. 退出B，回到原本机空间，年度回顾仍为3条。随后受限事务清理本次两个云端账号；09:36独立检查用户/会话/记录/来源/两类历史/幂等操作均为0，两项历史保护启用，才删除加密凭据文件。没有清除手机应用数据或其他账号缓存。

该项补齐单台真机账号隔离、显式选择导入与云端回读；邮箱本人验证及两台物理手机离线冲突仍待验收。原始已脱敏证据为 `.tools/acceptance-20260926/phone-account-validation.json`、`phone-cleanup-verification.json`；受控手机页面包括 `account-a-empty.xml`、`account-a-sync-result.xml`、`account-b-empty.xml`、`account-b-sync.xml`、`local-restored.xml`、`restored-local-summary.xml`。

## 阶段4：AI与自动复核前置检查

实时核对api v3和worker v2 ACTIVE；迁移001–005版本/名称与本地一致（未比较迁移内容哈希）；12张业务表RLS开启，8个核心RPC存在。邮箱注册启用且要求验证，但没有发送邮件，未声称投递成功。

仅检查Secret名称：DASHSCOPE_API_KEY、QWEN_BASE_URL、QWEN_MODEL仍缺失。真实已认证请求返回503，符合未配置状态。pg_cron、pg_net及cron.job不存在，生产Cron未启用。保留用户暂缓配置的决定，不将夹具测试作为真实analyze/chat/search/extractor/周报或Cron→复核→通知→确认闭环。

## 验证、交付及下一阶段

本次20操作API合约、Cron脚本5项检查、26项AI边界及16项OTA发布器行为测试通过。执行 `bash android/gradlew -p android testDebugUnitTest lintDebug assembleDebug --max-workers=1`，5分16秒成功：98项JVM、0失败/错误/跳过，Lint为0错误、22警告，Debug APK构建通过。使用本项目临时B盘映射绕过中文路径；现有R盘属于其他项目，未更改。构建日志在 `.tools/acceptance-20260926/android-verification.log`。没有运行手机instrumentation，也没有将Debug包安装到用户手机。

文档链接、脱敏结果结构和凭据模式检查通过，`git diff --check`通过。独立审查指出的历史段落归属及“响应丢失”歧义均已修正：本次只是重放相同操作，未注入真实网络丢包。

阶段签名提交 `3032be732a64843c6713183b4f87d6c4f6e95a91` 已发布main（GitHub签名标记unknown_key，不称Verified）。本次 [CI 36207231994](https://github.com/amateurish-programmer/boomerang/actions/runs/36207231994) 成功：Android测试/静态检查/构建及contracts任务全部通过，后者包含隔离PostgreSQL迁移、双用户权限、研究队列与接口边界。本次push未触发模拟器矩阵，device为skipped，未算作通过；原矩阵证据见OTA报告。后续仅补录本段CI结果与进度，不改变已验证产品代码或APK。

本机主要闭环、单台真机账号切换、整机重启与626秒锁屏的数据恢复已补齐。两台真机离线冲突、本人邮件验证、跨日及厂商长期后台时效、第三方云盘/外部分享接收仍待各自条件；专用恢复记录将期限延期到2026-10-04，活跃系统通知由1条撤销为0条，之后软删除该合成项，原本机3条仍在。通知拒绝/旧通知跨账号路由/取消与删除撤销以及全文件替换仍按清单单独标记。百炼配置继续暂缓，未标记V1全量完成。

## 新恢复路径发现的刷新缺陷（已由0.4.2闭环修复）

锁屏已实际观察626秒（USB供电，期间检查为Asleep），用户解锁后原话、胶囊约定、4版历史均保留，通知中心版本1/2/4仍各一条；没有跨日或强制后台任务，不声称长期投递准点。

从本轮真实导出的备份中仅提取专用测试项，生成全新UUID的一条合成恢复文件，其他用户内容不落电脑磁盘。手机选择该文件后预览1条/0重复，实际导入1条、总数3→4，导入的两个历史版本及新增导入修订均存在，系统提醒也已投递。但回到镖库后搜索 `ACCEPT_RESTORE` 持续无结果；仅终止并重开本应用后，新记录立即可见。这是已确认缺陷，不能将恢复闭环标为完全通过。

源代码定位：ShellViewModel持有长期Room实例和观察流，ExtrasViewModel为导入另开并立即关闭实例，当前数据库工厂未启用实例间失效通知。新增 `BackupObservationTest` 使用实际工厂、独立预览/写入实例和已启动的观察者，写入后立即关闭，覆盖新增及替换、历史、账号隔离；先断言落库成功，再限时断言观察流收到更新。测试源码与现有必需回归先本地编译通过；云端API35随后在两条观察断言精确失败，写入和历史断言通过。修复交付单列于 [0.4.2报告](RESTORE-REFRESH-0.4.2.md)。修复分支 `codex/fix-restore-refresh`，不会以重启绕过问题作为修复结论。

0.4.2已云端发布、真机应用内升级，新增与同UUID替换恢复无需重开即显示，1–6版历史保留；专用项软删除后活跃提醒1→0、原3条仍在，详见 [0.4.2完整报告](RESTORE-REFRESH-0.4.2.md)。
